import React from 'react';

const Home = () => {
  return (
    <div>
      <h1>Home Page Dedicated to JS</h1>
      <button onClick={() => console.log("Hi")}>Count</button>
    </div>
  )
}

export default Home;
